#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    return 0;
}
//원래 메소드는 데이터를 잡아 먹지 않지만 버츄얼(v)를 선언한 경우 데이터를 가진다
//멤버변수(변수) 멤버함수(속성)
//어떠한 행동을 할때 영향을 받은 쪽이 알아야함(맞았다<win> vs 때렸다)
//커플링이 있으면 객체지향을 만들때 좋지 않다. (디커플링 지향)
// 상속경우 꼭 커플링이 되기 때문에 포함(has)를 주로 사용한다
//.h(헤더파일) 클래스의 인터페이스를 선언 - 껍데기 [ include 중에 사용]
// .cpp(코드) 정의 + 실제구현 [gcc중에 사용/ 컴파일시 사용됨]
// 
